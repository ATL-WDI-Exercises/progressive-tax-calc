'use strict';

function getTaxAmount(income) {
  // TODO: implement algorithm here.
  return 0;
}

console.log('The tax on $100.00   is $' + getTaxAmount(100.00));
console.log('The tax on $30000.00 is $' + getTaxAmount(30000.00));
console.log('The tax on $50000.00 is $' + getTaxAmount(50000.00));
console.log('The tax on $90000.00 is $' + getTaxAmount(90000.00));
